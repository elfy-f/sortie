{% extends 'base.html.twig' %}

{% block title %}
 Les sorties
{% endblock %}

{% block body %}
 <section class="sorties-list">
  {% for sortie in sortie %}
  <article>
  <a href="{{ path('details', {'id': sortie.id}) }}" title="{{ sortie.name }} sortie/details {{ sortie.user|length }}user"></a>
  </article>
 </section>
{% endfor %}
{% endblock %}

