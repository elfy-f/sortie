{% extends 'base.html.twig' %}

{% block body %}
    <div>
        {{ form_start (sortieForm) }}

        {{ form_widget(sortieForm) }}

        <button>créer</button>

        {{ form_end(sortieForm) }}
    </div>
    <h1>les sorties</h1>
{% endblock %}